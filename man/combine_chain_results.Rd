% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chains.R
\name{combine_chain_results}
\alias{combine_chain_results}
\title{Combine the per-chain outputs of multiple \code{sample_chain()} calls.}
\usage{
combine_chain_results(per_chain_results)
}
\arguments{
\item{per_chain_results}{A list of \code{sample_chain()} return values, typically
produced by \code{future.apply::future_lapply(seq_len(n_chain), sample_chain, ...)}. All elements must have identical names.}
}
\value{
A named list. Each name is one of the field names appearing in
\code{per_chain_results[[1]]} (for example \code{final_state}, \code{traces},
\code{statistics}) and each value is a list of length \code{length(per_chain_results)}
containing that field's value for each chain in order.
}
\description{
When running multiple chains in parallel via e.g.
\code{future.apply::future_lapply()}, the return value is a list of length
\code{n_chain} where each element is itself the list returned by
\code{\link[=sample_chain]{sample_chain()}} (with entries such as \code{final_state}, \code{traces},
\code{statistics}, ...). \code{combine_chain_results()} inverts this nesting so that
the output is a single list keyed by the original result-field names, with
each entry being a length-\code{n_chain} list holding the per-chain values.
}
\details{
The output preserves the per-chain matrix shape of \code{traces} and
\code{statistics}; downstream tooling (e.g. the \code{posterior} package) can then
choose how to stack them into arrays.
}
\examples{
target_distribution <- list(
  log_density = function(x) -sum(x^2) / 2,
  gradient_log_density = function(x) -x,
  dimension = 2
)
per_chain <- lapply(1:3, function(chain_index) {
  withr::with_seed(chain_index, {
    sample_chain(
      chain_index = chain_index,
      target_distribution = target_distribution,
      initial_state = stats::rnorm,
      n_warm_up_iteration = 100,
      n_main_iteration = 100,
      show_progress_bar = FALSE
    )
  })
})
combined <- combine_chain_results(per_chain)
names(combined)
length(combined$traces)
}

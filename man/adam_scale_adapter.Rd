% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adaptation.R
\name{adam_scale_adapter}
\alias{adam_scale_adapter}
\title{Create object to adapt proposal scale to coerce average acceptance rate
using the Adam optimizer of Kingma and Ba (2014).}
\usage{
adam_scale_adapter(
  initial_scale = NULL,
  target_accept_prob = NULL,
  learning_rate = 0.05,
  beta_1 = 0.9,
  beta_2 = 0.999,
  epsilon = 1e-08,
  learn_rate_decay = 0.5
)
}
\arguments{
\item{initial_scale}{Initial value to use for scale parameter. If not set
explicitly a proposal and dimension dependent default will be used.}

\item{target_accept_prob}{Target value for average accept probability for
chain. If not set a proposal dependent default will be used.}

\item{learning_rate}{Learning rate for Adam optimizer (the \code{alpha}
hyperparameter in the original Adam paper). Controls the magnitude of the
update applied to the log-scale on each iteration. Should be positive.
Defaults to \code{0.05}, which is a practical setting that gives fast
convergence of the acceptance-rate coercion within typical MCMC warm-up
lengths; the original Adam paper uses \code{1e-3}.}

\item{beta_1}{Exponential decay rate for the first-moment estimate of the
gradient (the \code{beta_1} hyperparameter in the original Adam paper). Should
be in \verb{[0, 1)}. Defaults to \code{0.9}.}

\item{beta_2}{Exponential decay rate for the second-moment (squared
gradient) estimate (the \code{beta_2} hyperparameter in the original Adam
paper). Should be in \verb{[0, 1)}. Defaults to \code{0.999}.}

\item{epsilon}{Small positive constant added to the square root of the
second-moment estimate in the denominator of the Adam update for
numerical stability. Defaults to \code{1e-8}.}

\item{learn_rate_decay}{Exponent controlling the decay of the effective
learning rate across iterations: on iteration \code{t} the effective learning
rate is \code{learning_rate / t^learn_rate_decay}. Should be in \verb{[0, 1]}. A
value of \code{0} recovers standard Adam; the default of \code{0.5} matches the
value recommended in the \code{walnuts} implementation.}
}
\value{
List of functions with entries
\itemize{
\item \code{initialize}, a function for initializing adapter state and proposal
parameters at beginning of chain,
\item \code{update} a function for updating adapter state and proposal parameters on
each chain iteration,
\item \code{finalize} a function for performing any final updates to adapter state and
proposal parameters on completion of chain sampling (may be \code{NULL} if
unused).
\item \code{state} a zero-argument function for accessing current values of adapter
state variables.
}
}
\description{
Applies the Adam stochastic-gradient optimizer to the log of the proposal
scale, using the acceptance-rate residual \code{target_accept_prob - accept_prob}
as the (stochastic) gradient. This corresponds to treating
\code{-0.5 * (accept_prob - target_accept_prob)^2} as the objective and is the
same gradient signal used by \code{\link[=dual_averaging_scale_adapter]{dual_averaging_scale_adapter()}} and
\code{\link[=stochastic_approximation_scale_adapter]{stochastic_approximation_scale_adapter()}}, but plugged into the Adam
update rule instead of a dual-averaging or Robbins-Monro schedule. Follows
the reference implementation in the \code{walnuts} library (see references
below).
}
\details{
To match the stability provided by dual averaging, a learning-rate decay
\code{learning_rate / t^learn_rate_decay} is applied to the per-iteration Adam
step; a value of \code{learn_rate_decay = 0} recovers standard Adam, while
\code{learn_rate_decay = 0.5} (the default here) is recommended by the \code{walnuts}
authors for stable convergence.
}
\examples{
proposal <- barker_proposal()
adapter <- adam_scale_adapter(
  initial_scale = 1., target_accept_prob = 0.4
)
adapter$initialize(proposal, chain_state(c(0, 0)))
}
\references{
Kingma, D. P., & Ba, J. (2014). Adam: A method for stochastic
optimization. \emph{arXiv preprint} arXiv:1412.6980.

Reference implementation in \code{walnuts}:
\url{https://github.com/flatironinstitute/walnuts/blob/main/include/walnuts/adam.hpp}
}
